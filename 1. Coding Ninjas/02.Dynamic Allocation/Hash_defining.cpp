#include<iostream>
#define PI 3.14 
using namespace std;

// # define is used for maintaing cleaner code
// We dont have to change the value in compleate code instead we can change it at initialisation
// # define is lil bit faster than normal variable

// Note: We dont use "=" or ";" in #define
int main() {
    cout << PI + 0 << endl;
}